{
  "name": "safeway-app",
  "engines": {
  "node": "22.x"
},
  "private": true,
  "workspaces": [
    "frontend",
    "orchestrator"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend && npm run build --workspace=orchestrator"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
